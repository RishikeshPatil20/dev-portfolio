<!-- <%- include('partials/header') %>
<section class="home">
  <div class="hero-text">
    <h2>Hi, I'm Rishikesh</h2>
    <p>Developer | Designer | Learner</p>
    <a href="/projects" class="btn">See My Work</a>
  </div>
</section>
<%- include('partials/footer') %> -->
<%- include('partials/header') %>
<section class="experience-section">
  <div class="container">
    <div class="experience-intro">
      <h2>Projects</h2>
      <p>Here's a timeline of my professional journey, showcasing my contributions and projects in full-stack development.</p>
    </div>

    <div class="timeline-container">
      <div class="timeline-line"></div> <% /* This is the fixed line */ %>
      <div class="timeline-line-fill"></div> <% /* This line fills on scroll */ %>

      <% 
        const experiences = [
         {
            title: 'Connect-Sphere',
            softwaredevelopment: 'Backend Project',
            status: 'completed',
            description: 'The ConnectSphere project is a secure and interactive social media platform built with Node.js, Express.js, MongoDB, and Mongoose. Users can create posts, like, comment, and manage accounts. Designed with modular architecture, it ensures security with JWT authentication and role-based access control, offering a scalable and efficient experience.',
              tech: [
                'Node.js',
                'Express.js',
                'MongoDB',
                'Mongoose',
                'JWT',
                'Bcrypt',
                'Multer', 
                'Nodemailer',
                'Twilio', 
                'OTP Generator', 
                'Validator.js'
            ],
            links: [
                { icon: 'fab fa-github', text: 'GitHub', url: 'https://github.com/RishikeshPatil20/ConnectSphere' }
            ]
          },
           {
            title: 'HireHive -Job Portal Project',
            softwaredevelopment: 'Full-Stack Project',
            status: 'Current',
            description: 'A dynamic job portal web application built with Express.js and EJS, enabling job seekers to explore opportunities and recruiters to manage job postings efficiently. Features include user authentication, file uploads, and a modular MVC structure.',
            tech: [
                'Express.js',
                'EJS',
                'Node.js',
                'Multer',
                'Express Session',
                'Cookie Parser',
                'Validator.js',
                'UUID'
            ],
            links: [
                { icon: 'fab fa-github', text: 'GitHub', url: 'https://github.com/RishikeshPatil20/Hire-Hive' }
            ]
          },
           {
            title: 'chatter-up',
            softwaredevelopment: 'Real-time Chat Application',
            status: 'Completed',
            description: 'This project is a real-time social media chat application built using Node.js, Express.js, MongoDB, and Socket.IO. Users can create accounts, send and receive messages, view online/offline statuses, and see typing indicators in a live chat interface.',
            tech: [
                'Node.js',
                'Express.js',
                'MongoDB',
                'Mongoose',
                'Socket.IO',
                'CORS'
            ],
            links: [
                { icon: 'fab fa-github', text: 'GitHub', url: 'https://github.com/RishikeshPatil20/chatter-up' },
                // { icon: 'fas fa-globe', text: 'Live Demo', url: 'YOUR_LIVE_DEMO_URL' }
            ]
          },
           {
            title: 'stock-market',
            softwaredevelopment: 'Frontend Project',
            status: 'Completed',
            description: 'Track and analyze stock market trends with real-time data and interactive charts. Built with HTML, CSS, JavaScript, and leveraging data from a third-party API, our intuitive stock market platform provides company summaries, profit margins, and more.',
            tech: ['HTML', 'CSS', 'JavaScript', 'Third-Party API Integration', 'Real-time Data'],
            links: [
                { icon: 'fas fa-github', text: 'Website', url: 'https://github.com/RishikeshPatil20/stock-market' },
            ]
          },
      {
            title: 'Movie-Booking',
            softwaredevelopment: 'Frontend Project',
            status: 'Completed',
            description: 'An interactive movie ticket booking system built with HTML, CSS, and JavaScript, featuring intuitive seat selection, pricing, and instant updates.',
            tech: ['HTML', 'CSS', 'JavaScript'],
            links: [
              { icon: 'fas fa-github', text: 'Website', url: 'https://github.com/RishikeshPatil20/Movie-Booking' },
            ]
          },
        ];
      %>

      <% experiences.forEach((exp, index) => { %>
        <div class="timeline-item">
          <div class="timeline-icon"></div> <% /* Circle icon for the timeline */ %>
          <div class="experience-card">
            <h3><%= exp.title %></h3>
            <p class="company"><%= exp.softwaredevelopment %> <span class="status">(<%= exp.status %>)</span></p>
            <p class="description"><%= exp.description %></p>
            <div class="tech-stack">
              <% exp.tech.forEach(tech => { %>
                <span><%= tech %></span>
              <% }); %>
            </div>
            <% if (exp.links && exp.links.length > 0) { %>
              <div class="card-links">
                <% exp.links.forEach(link => { %>
                  <a href="<%= link.url %>" target="_blank" rel="noopener noreferrer">
                    <i class="<%= link.icon %>"></i> <%= link.text %>
                  </a>
                <% }); %>
              </div>
            <% } %>
          </div>
        </div>
      <% }); %>
    </div>
  </div>
</section>

<%- include('partials/footer') %>

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
<script src="/js/timeline.js"></script>


//----------------------

/* body {
  font-family: 'Segoe UI', sans-serif;
  margin: 0;
  background-color: #f7f7f7;
  color: #333;
}

header {
  background-color: #111;
  padding: 1rem;
  color: white;
}

nav {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.nav-links {
  list-style: none;
  display: flex;
  gap: 1rem;
}

.nav-links a {
  color: white;
  text-decoration: none;
}

.hero-text {
  text-align: center;
  padding: 4rem 1rem;
}

.btn {
  background-color: #007BFF;
  padding: 0.75rem 1.5rem;
  color: white;
  border: none;
  text-decoration: none;
  display: inline-block;
  margin-top: 1rem;
}

.projects, .about, .contact {
  padding: 2rem;
}

.project-card {
  border: 1px solid #ccc;
  padding: 1rem;
  margin: 1rem 0;
} */

body {
  font-family: 'Segoe UI', sans-serif;
  margin: 0;
  background-color: var(--background);
  color: var(--text-primary);
  background-image:
    linear-gradient(to right, var(--card-border) 1px, transparent 1px),
    linear-gradient(to bottom, var(--card-border) 1px, transparent 1px);
  background-size: 44px 44px;
  overflow-x: hidden;
}

:root {
  --background: #0a0a0a;
  --foreground: #ededed;
  --text-primary: #fff;
  --text-secondary: #8b8b8b;
  --text-muted: #666;
  --card-background: #1a1a1a;
  --card-border: #232323;
  --hover-background: #242424;
}

header {
  background-color: var(--card-background);
  padding: 1rem;
  color: var(--text-primary);
}

nav {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.nav-links {
  list-style: none;
  display: flex;
  gap: 1rem;
}

.nav-links a {
  color: var(--text-primary);
  text-decoration: none;
}

.hero-text {
  text-align: center;
  padding: 4rem 1rem;
}

.hero-text h2 {
    color: var(--foreground);
}

.hero-text p {
    color: var(--text-secondary);
}

.btn {
  background-color: #007BFF;
  padding: 0.75rem 1.5rem;
  color: var(--text-primary);
  border: none;
  text-decoration: none;
  display: inline-block;
  margin-top: 1rem;
  transition: background-color 0.3s ease;
}

.btn:hover {
    background-color: #0056b3;
}

.projects, .about, .contact {
  padding: 2rem;
}

.project-card {
  border: 1px solid var(--card-border);
  background-color: var(--card-background);
  padding: 1rem;
  margin: 1rem 0;
  border-radius: 8px;
  transition: background-color 0.3s ease, border-color 0.3s ease;
}

.project-card:hover {
    background-color: var(--hover-background);
    border-color: var(--foreground);
}

.project-card p {
    color: var(--text-secondary);
}

footer {
    background-color: var(--card-background);
    color: var(--text-muted);
    text-align: center;
    padding: 1rem;
    margin-top: 2rem;
    width: auto;
}

/* ----- */

/* --- Experience Section Specific Styles --- */
.experience-section {
  padding: 4rem 1rem;
  max-width: 960px;
  margin: 0 auto;
}

.experience-intro {
  text-align: center;
  margin-bottom: 3rem;
}

.experience-intro h2 {
  font-size: 2.5rem;
  color: var(--foreground);
  margin-bottom: 0.5rem;
}

.experience-intro p {
  font-size: 1.1rem;
  color: var(--text-secondary);
  line-height: 1.6;
}

.timeline-container {
  position: relative;
  padding-left: 25px; /* Space for the vertical line and icons */
  width: 100%;
  min-height: 100%;

}


.timeline-line {
  position: absolute;
  left: 10px;
  top: 0;
  bottom: 0;
  width: 2px;
  background-color: var(--card-border); /* The static thin line */
  z-index: 0;
}


.timeline-line-fill {
  position: absolute;
  left: 10px;
  top: 0;
  width: 2px;
  background: linear-gradient(to bottom, #007BFF, #00C1FF);
  z-index: 1;
  height: 0;
  transition: height 0.2s ease-in-out;
}


.timeline-item {
  position: relative;
  margin-bottom: 2rem;
  padding-left: 35px; /* Space for the icon + gap */
}

.timeline-icon {
  position: absolute;
  left: 0;
  top: 0.5rem; /* Adjust to vertically align with card title */
  width: 20px;
  height: 20px;
  background-color: var(--card-border); /* Icon background */
  border: 2px solid var(--card-background); /* Border to match card background */
  border-radius: 50%;
  z-index: 2; /* Ensure it's above the lines */
}

.experience-card {
  background-color: var(--card-background);
  border: 1px solid var(--card-border);
  border-radius: 8px;
  padding: 1.5rem;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.experience-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 8px 16px rgba(0, 0, 0, 0.4);
}

.experience-card h3 {
  color: var(--foreground);
  margin-top: 0;
  margin-bottom: 0.5rem;
  font-size: 1.4rem;
}

.experience-card .company {
  color: var(--text-secondary);
  font-size: 0.95rem;
  margin-bottom: 0.8rem;
}

.experience-card .company .status {
  color: var(--text-muted);
  font-style: italic;
}

.experience-card .description {
  color: var(--text-secondary);
  font-size: 1rem;
  line-height: 1.5;
  margin-bottom: 1rem;
}

.tech-stack {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
  margin-top: 1rem;
  margin-bottom: 1rem;
}

.tech-stack span {
  background-color: #333; /* Darker background for tech tags */
  color: var(--text-primary);
  padding: 0.3rem 0.8rem;
  border-radius: 4px;
  font-size: 0.85rem;
  white-space: nowrap;
}

.card-links {
  margin-top: 1rem;
  display: flex;
  flex-wrap: wrap;
  gap: 1rem;
}

.card-links a {
  color: var(--text-secondary);
  text-decoration: none;
  font-size: 0.9rem;
  display: flex;
  align-items: center;
  gap: 0.4rem;
  transition: color 0.3s ease;
}

.card-links a:hover {
  color: var(--foreground); /* Highlight links on hover */
}

.card-links a i {
  color: #007BFF; /* Icon color, can use a variable */
}

/* Responsive adjustments */
@media (max-width: 768px) {
  .experience-section {
    padding: 3rem 1rem;
  }
  .experience-intro h2 {
    font-size: 2rem;
  }
  .experience-card {
    padding: 1rem;
  }
  .experience-card h3 {
    font-size: 1.2rem;
  }
  .tech-stack span {
    font-size: 0.75rem;
  }
}
/* Header and Navigation Styles */
header {
  padding: 1rem;
  color: var(--text-primary);
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  z-index: 1000;
  background-color: rgba(10, 10, 10, 0);
  transition: background-color 0.3s ease;
}

nav {
  display: flex;
  justify-content: space-between; /* Distributes space between logo, nav-links, and social-links */
  align-items: center;
  max-width: 1200px;
  margin: 0 auto;
}

.logo {
  color: var(--text-primary);
  font-size: 1.5rem;
  margin: 0;
}

.nav-links {
  list-style: none;
  display: flex;
  gap: 1.5rem;
  margin: 0;
  padding: 0;
  flex-grow: 1; /* Allow nav-links to take up available space */
  justify-content: center; /* Center the nav links within their available space */
}

.nav-links a {
  color: var(--text-primary);
  text-decoration: none;
  font-weight: 500;
  transition: color 0.3s ease;
}

.nav-links a:hover {
  color: var(--foreground);
}

/* NEW: Styles for social links in header */
.header-social-links {
    display: flex;
    gap: 1rem; /* Smaller gap between icons in the header */
    align-items: center;
}

.header-social-links a {
    color: var(--text-secondary); /* Default icon color */
    font-size: 1.5rem; /* Smaller icon size as requested (was 2.5rem) */
    transition: color 0.3s ease, transform 0.2s ease;
    text-decoration: none;
}

.header-social-links a:hover {
    color: var(--foreground);
    transform: translateY(-3px); /* Slightly less lift for smaller icons */
}

/* --- Existing Home Section (no change needed here usually) --- */
.home {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  padding-top: 60px; /* Adjust if header height changes */
  text-align: center;
}

.hero-text {
  padding: 4rem 1rem 2rem;
}

/* --- About Me Section (original styles from index.ejs that you included in the prompt) --- */
.about-hero {
    max-width: 800px;
    padding: 2rem 1rem;
    margin-top: 3rem;
    background-color: var(--card-background);
    border: 1px solid var(--card-border);
    border-radius: 8px;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
    text-align: center;
}

.about-content h3 {
    font-size: 2rem;
    color: var(--foreground);
    margin-bottom: 1rem;
}

.about-content p {
    font-size: 1.1rem;
    color: var(--text-secondary);
    line-height: 1.7;
    margin-bottom: 2rem; /* Keep this if you like the space */
    max-width: 700px;
    margin-left: auto;
    margin-right: auto;
}

/* REMOVE these social-links styles as they are no longer in about-hero */
/*
.social-links {
    display: flex;
    justify-content: center;
    gap: 2rem;
    margin-top: 2rem;
}

.social-links a {
    color: var(--text-secondary);
    font-size: 2.5rem;
    transition: color 0.3s ease, transform 0.2s ease;
    text-decoration: none;
}

.social-links a:hover {
    color: var(--foreground);
    transform: translateY(-5px);
}
*/